# Copy this file to .secrets and fill in real values.
# Used by the GitHub Local Actions extension / act when running workflows locally.
# .secrets is gitignored — never commit real values.

GITHUB_TOKEN=

# Push credentials for registry.anekdote.in (used by .github/workflows/image-publish.yml).
REGISTRY_USERNAME=
REGISTRY_PASSWORD=
